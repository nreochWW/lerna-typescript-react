version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15-jessie
    steps:
      - checkout
      - restore_cache:
          name: Restore Yarn Package Cache
          keys:
            - yarn-packages-{{ checksum "yarn.lock" }}
      - run:
          name: Install Dependencies
          command: yarn install --frozen-lockfile
      - save_cache:
          name: Save Yarn Package Cache
          key: yarn-packages-{{ checksum "yarn.lock" }}
          paths:
            - ~/.cache/yarn
      - run:
          name: Format
          run: yarn prettier
#   test:
#     docker:
#       - image: circleci/node:10.15-jessie
#     steps:
#       - checkout
#       - run:
#           name: Test
#           command: yarn test

# workflows:
#   version: 2
#   lint_and_test:
#     jobs:
#       - lint
#       - test:
#           requires:
#             - lint
#           filters:
#             branches:
#               only: master
